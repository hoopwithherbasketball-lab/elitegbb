{
  "name": "elitegbb-connectgbb-build-root",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Pages root-directory wrapper for legacy CONNECTGBB setting",
  "scripts": {
    "build": "cd ../frontend && npm ci --legacy-peer-deps && DISABLE_ESLINT_PLUGIN=true npm run build && rm -rf ../CONNECTGBB/build && cp -R build ../CONNECTGBB/build",
    "pages:build": "npm run build && npm run verify:artifact",
    "prepages:build": "node -e \"const fs=require('fs'); const t=fs.readFileSync('wrangler.toml','utf8'); if(!/pages_build_output_dir\\s*=\\s*\\\"build\\\"/.test(t)){console.error('wrangler pages_build_output_dir must be \"build\"'); process.exit(1)}\"",
    "verify:artifact": "test -f build/index.html"
  "name": "connectgbb",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "NEXT_DISABLE_TURBOPACK=1 NEXT_FORCE_WEBPACK=1 next build",
    "start": "next start",
    "lint": "eslint",
    "pages:build": "NEXT_DISABLE_TURBOPACK=1 NEXT_FORCE_WEBPACK=1 node_modules/.bin/opennextjs-cloudflare build && cp .open-next/worker.js .open-next/_worker.js",
    "pages:preview": "npx @opennextjs/cloudflare preview"
  },
  "dependencies": {
    "@stripe/stripe-js": "^9.0.1",
    "@supabase/supabase-js": "^2.101.0",
    "next": "16.2.1",
    "react": "19.2.4",
    "react-dom": "19.2.4",
    "stripe": "^21.0.1"
  },
  "devDependencies": {
    "@opennextjs/cloudflare": "^1.5.0",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.1",
    "esbuild": "^0.25.3",
    "tailwindcss": "^4",
    "typescript": "^5",
    "wrangler": "^4.52.1"
  }
}
